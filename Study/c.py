import re


print(re.findall("hello", 'oi hello sagar, i am here, hello there upasana'))

# find searches for the first one
# match searches it in the initial only
# findall searches all the string, and returns the list of the found ones
